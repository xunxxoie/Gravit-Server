services:
  prometheus:
    image: prom/prometheus
    container_name: gravit_prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./infra/prometheus.yml:/etc/prometheus/prometheus.yml
    networks:
      - gravit-local

  grafana:
    image: grafana/grafana
    container_name: gravit_grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - ./grafana_data:/var/lib/grafana
    networks:
      - gravit-local
    depends_on:
      - prometheus
      - loki
    user: root

  loki:
    image: grafana/loki:2.9.8
    container_name: gravit_loki
    ports:
      - "3100:3100"
    command: -config.file=/etc/loki/local-config.yml
    volumes:
      - ./infra/loki-config.yml:/etc/loki/local-config.yml:ro
    networks:
      - gravit-local

networks:
  gravit-local:
    name: gravit-local
